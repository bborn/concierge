<h1>Delivery audit log</h1>
<table>
  <thead><tr><th>Agent</th><th>Account</th><th>Channel</th><th>Kind</th><th>Sent at</th></tr></thead>
  <tbody>
    <% @deliveries.each do |d| %>
      <tr>
        <td><%= d.agent_slug %></td>
        <td><%= subject_label(d) %></td>
        <td><%= d.channel %></td>
        <td><%= d.kind %></td>
        <td><%= d.sent_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
